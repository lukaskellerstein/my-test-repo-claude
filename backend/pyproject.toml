[project]
name = "todo-backend"
version = "0.1.0"
description = "Simple TODO app backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.113.0",
    "uvicorn>=0.25.0",
]

[tool.uv]
dev-dependencies = []
